#include <stdio.h>

int main() {
	float radius;
	printf("entre raidus");
	scanf("%f",&radius);
	float pi = 3.1415;
	float area = pi * radius * radius ;
	printf("the area of the circle is : %f",area);
	return 0;
}
